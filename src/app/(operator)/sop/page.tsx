import { ContentList } from "@/components/operator/content-list";

export default function SopListPage() {
  return <ContentList contentType="SOP" detailPath="sop" title="Procedure operative (SOP)" />;
}
